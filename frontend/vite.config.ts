import { defineConfig } from 'vite';

export default defineConfig({
  // Hier definieren wir, wo nach der .env-Datei gesucht werden soll
  envDir: '../',
  
  // Falls du React, Vue oder andere Plugins nutzt, kommen sie hier rein
  plugins: [],
});